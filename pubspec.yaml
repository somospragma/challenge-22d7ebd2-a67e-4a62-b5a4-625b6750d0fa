name: notification_push_challenge
description: Implementación de notificaciones push para iOS y Android.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_messaging: ^15.1.0
  flutter_local_notifications: ^17.0.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.24.0

flutter:
  uses-material-design: true
